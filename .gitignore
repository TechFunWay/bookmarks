# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test
# SQLite test databases produced by the unit tests
*.db
*.db-wal
*.db-shm

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Build artifacts
*.zip

# Dependency directories (remove the comment below to include it)
# vendor/
.playwright-mcp/
.omo/
.development-note/
.env
.idea
.DS_Store
data.db
# 其它 AI 工具的本地目录，不纳入版本管理（打包逻辑已迁至 .development-note/skills/release-package）
.development-note/
.development-note/
/static/icons
/static/downloads
.vscode
release
/logs
/data
/bookmarks
bookmarks
bookmark
techfunway.bookmarks/app/server/bookmarks
techfunway.bookmarks/app/server/reset-password
techfunway.bookmarks/app/server/edge-extension.zip
# Playwright 测试产物
test-results/
